add http response codes

fix random uniqe num for striprefe

adding filters for handling update expration date

setup the identity server separately from UI (or setup identity server with API) so that in future if there is mobile app then can share and use the api and identity server together


shopUI:
for adminn -> controller in UI -> class in app -> java script using controller route (vue) -> razor page .cshtml



pages code behind are calling the classes